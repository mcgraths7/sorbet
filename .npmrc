# Auto-install a package's peerDependencies (pnpm's common default) so the React
# peer of @sorbet/component-library resolves during local development without
# each consumer package re-declaring it.
auto-install-peers=true
